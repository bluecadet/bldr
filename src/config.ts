import { ConfigSettings, LocalConfigSettings } from "./lib/@types/configTypes";

export function bldrConfig(config: ConfigSettings): ConfigSettings {
  return config;
}


export function bldrLocalConfig(config: LocalConfigSettings): LocalConfigSettings {
  return config;
}