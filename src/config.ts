import { ConfigSettings } from "./lib/@types/configTypes";

export function bldrConfig(config: ConfigSettings): ConfigSettings {
  return config;
}


export function bldrLocalConfig(config: ConfigSettings): ConfigSettings {
  return config;
}