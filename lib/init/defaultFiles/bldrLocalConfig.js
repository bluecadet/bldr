/**
 * Bldr Local Config
 *
 * npm i --save-dev @bluecadet/bldr
 *
 * run `bldr dev`, `bldr watch`, or `bldr build`
 *
 * !!------------------------------------------------------------!!
 *
 * This local config file should be ignored by git.
 *
 * Create browsersync options, such as port, proxy server, etc.
 *
 * !!------------------------------------------------------------!!
 *
 */

module.exports = {
  browserSync: {
    // port: 3000,
    // proxy: 'http://site.local'
  }
}